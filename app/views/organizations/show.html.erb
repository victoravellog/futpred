<div class="min-h-screen bg-gray-50">
  <%= render "shared/navbar" %>

  <main class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
    <% if notice %>
      <div class="mb-4 bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded">
        <%= notice %>
      </div>
    <% end %>

    <div class="flex justify-between items-center mb-6">
      <div>
        <h1 class="text-2xl font-bold text-gray-900"><%= @organization.name %></h1>
        <p class="text-gray-500"><%= @organization.users.count %> miembros</p>
      </div>
      <%= link_to "Nuevo torneo", new_organization_tournament_path(@organization),
          class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700" %>
    </div>

    <% if @tournaments.any? %>
      <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
        <% @tournaments.each do |tournament| %>
          <%= link_to tournament_path(tournament), class: "block bg-white overflow-hidden shadow rounded-lg p-6 hover:shadow-md transition-shadow" do %>
            <h3 class="text-lg font-medium text-gray-900"><%= tournament.name %></h3>
            <p class="text-sm text-gray-500 mt-1">
              <%= tournament.rounds.count %> rondas ·
              <%= tournament.fixtures.count %> partidos
            </p>
          <% end %>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-12 bg-white rounded-lg shadow">
        <p class="text-gray-500 mb-4">No hay torneos en esta organización todavía.</p>
        <%= link_to "Crear primer torneo", new_organization_tournament_path(@organization),
            class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700" %>
      </div>
    <% end %>
  </main>
</div>
